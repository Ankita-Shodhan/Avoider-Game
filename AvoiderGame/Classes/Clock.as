package
{
	import flash.display.MovieClip;
	public class Clock extends Game
	{
		public var gamePlayedTime: Number=0;
		public function Clock()
		{
			super();
		}
		
		public function updateDisplay():void
		{
			/*var frameToSkipTo:Number = currentValue / 1000;			// Convert it in milli seconds
			frameToSkipTo = Math.floor( frameToSkipTo );
			frameToSkipTo = frameToSkipTo + 1;
			while ( frameToSkipTo > 12 )
			{
				frameToSkipTo = frameToSkipTo - 12;
			}
			this.gotoAndStop( frameToSkipTo );*/
		}
	}
}